# Data Sources Registry

> **Purpose:** registry of research data sources used by `/research` and related skills.
>
> **Status:** populated per operation. Empty at the template level.

Each source is logged with: name, access method (API / MCP / scrape / login), auth pattern, rate limit, freshness, evidence-hierarchy rank (per `../frameworks/market-research-method.md`).

Referenced from `../../skills/research/SKILL.md` Decision Logic.
