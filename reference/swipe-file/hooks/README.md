# Swipe File — hooks

> **Purpose:** proven, high-performing hooks examples with performance data. Used as calibration references by skills that produce similar asset types.
>
> **Status:** populated per operation. Empty at the template level.

Each file here MUST include:
- Source attribution (operator / brand name)
- Performance data (views / engagement / conversion / revenue generated)
- Capture date
- Tags (vertical / audience / awareness level / mechanism)

Do not include swipes without performance data. Unvalidated swipes belong in `../../operator-research/`.
