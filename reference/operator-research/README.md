# Operator Research

> **Purpose:** raw research notes + source material for operators being encoded into `../operators/` and `../frameworks/<operator>/`.
>
> **Status:** staging area. Raw notes live here; encoded content lives in `../operators/` + `../frameworks/`.

Typical contents (once populated):
- Transcripts of operator content (YouTube, podcasts, calls)
- Screenshot captures of their funnels / VSLs
- Copy-paste swipes before structured encoding
- Breakdown WIP files before they become canonical framework docs

Do NOT cite files here from `SKILL.md` bodies. Cite the canonical `../operators/<name>.md` + `../frameworks/<operator>/` instead.
