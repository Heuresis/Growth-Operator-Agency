# `.claude/commands/` — Claude Code slash-command shims

One `.md` per skill. Each file wires a slash command (e.g. `/research`, `/build-vsl`) into the corresponding `skills/<slug>/adapters/claude-code.md` runtime.

Read first: [`docs/QUICKSTART.md`](../../docs/QUICKSTART.md) · [`docs/ARCHITECTURE.md`](../../docs/ARCHITECTURE.md).
