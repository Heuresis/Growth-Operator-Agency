---
description: Execute the content-calendar skill per Cycle 3 Attention
allowed-tools: Read, Write, Edit, Grep, Glob, Bash, WebFetch, WebSearch
---

# /content-calendar

Read and execute `skills/content-calendar/SKILL.md`. Follow its Process section.

Write output to `output/content-calendar/{date}-{output}.md`.

$ARGUMENTS
