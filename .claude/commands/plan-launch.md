---
description: Execute plan-launch skill (Cycle 5/6 Scale + Deploy + Partnerships)
allowed-tools: Read, Write, Edit, Grep, Glob, Bash, WebFetch, WebSearch
---

# /plan-launch

Read and execute `skills/plan-launch/SKILL.md`. Follow Process section.
Write output to `output/plan-launch/{date}-{output}.md`.

$ARGUMENTS
