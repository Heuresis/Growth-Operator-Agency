---
description: Execute the story-sequence skill per Cycle 3 Attention
allowed-tools: Read, Write, Edit, Grep, Glob, Bash, WebFetch, WebSearch
---

# /story-sequence

Read and execute `skills/story-sequence/SKILL.md`. Follow its Process section.

Write output to `output/story-sequence/{date}-{output}.md`.

$ARGUMENTS
