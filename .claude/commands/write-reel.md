---
description: Execute the write-reel skill per Cycle 3 Attention
allowed-tools: Read, Write, Edit, Grep, Glob, Bash, WebFetch, WebSearch
---

# /write-reel

Read and execute `skills/write-reel/SKILL.md`. Follow its Process section.

Write output to `output/write-reel/{date}-{output}.md`.

$ARGUMENTS
